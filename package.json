{
  "name": "js-3d-engine",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/electron/electron.js",
  "scripts": {
    "start": "npm run build && electron .",
    "dev": "npm run electron && concurrently -k \"vite\" \"wait-on tcp:5173 && electron . --dev\"",
    "build": "npm run electron && vite build",
    "electron": "tsc --p tsconfig.json",
    "vite:build": "vite build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "concurrently": "^9.2.1",
    "electron": "^41.1.1",
    "typescript": "^5.3.3",
    "vite": "^8.0.5",
    "wait-on": "^9.0.4"
  }
}
